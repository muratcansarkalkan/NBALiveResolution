#pragma once

#include <string>

namespace adjustWSUI {
    void adjustWidescreenUI(float value, const std::string& pathA, const std::string& pathB);
} // namespace for custom widescreen UI components
